# Plan: Fix CI

Fix lint errors and warnings reported by Biome in `src/components/carousel/carousel.tsx`.

## Tasks

- [x] Fix `useExhaustiveDependencies` errors in `src/components/carousel/carousel.tsx`.
- [x] Fix `noStaticElementInteractions` error in `src/components/carousel/carousel.tsx`.
- [x] Resolve `noExplicitAny` warnings in `src/components/carousel/carousel.tsx`.
- [x] Verify CI by running `npm run lint`, `npm run type-check`, and `npm test`.

## Design

### 1. Fix `useExhaustiveDependencies`
- Add `itemCount` and `selectedIndex` to the dependency array of `useLayoutEffect`.

### 2. Fix `noStaticElementInteractions`
- Add `role="region"` or similar to the interactive `div`, or use a more appropriate semantic element. Since it's a scroll container, `role="list"` might be better if children are items. Or simply add a role.

### 3. Resolve `noExplicitAny`
- Use more specific types instead of `any`.

## Implementation

### 1. `src/components/carousel/carousel.tsx`
- Update `useLayoutEffect` dependency array.
- Add `role` attribute to the container `div`.
- Replace `any` with `React.ReactElement` and appropriate child type.

## Verification

- Run `npm run lint`.
- Run `npm run type-check`.
- Run `npm run test`.
- Run `npm run build`.
